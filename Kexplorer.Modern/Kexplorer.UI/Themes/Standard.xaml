<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:system="clr-namespace:System;assembly=mscorlib">
    <!-- Marker key to identify theme dictionaries -->
    <system:Boolean x:Key="IsThemeDictionary">True</system:Boolean>

    <!-- Standard theme: formalizes the default WPF appearance -->
    <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#F5F5F5" />
    <SolidColorBrush x:Key="AlternateRowBrush" Color="#F9F9F9" />
    <SolidColorBrush x:Key="PrimaryForegroundBrush" Color="#1E1E1E" />
    <SolidColorBrush x:Key="SecondaryForegroundBrush" Color="#6E6E6E" />
    <SolidColorBrush x:Key="AccentBrush" Color="#0078D4" />
    <SolidColorBrush x:Key="ThemeBorderBrush" Color="#CCCCCC" />
    <SolidColorBrush x:Key="ErrorBrush" Color="#E81123" />
    <SolidColorBrush x:Key="SuccessBrush" Color="#107C10" />
    <SolidColorBrush x:Key="ToolbarBackgroundBrush" Color="#F5F5F5" />
    <SolidColorBrush x:Key="ScrollbarThumbBrush" Color="#999999" />
    <SolidColorBrush x:Key="SelectedTabBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="SelectionBrush" Color="#B8C4DC" />
    <SolidColorBrush x:Key="SelectionForegroundBrush" Color="#1E1E1E" />
    <SolidColorBrush x:Key="TitleBarBackgroundBrush" Color="#F0F0F0" />
    <SolidColorBrush x:Key="TitleBarForegroundBrush" Color="#1E1E1E" />

    <!-- Standard TabItem style -->
    <Style TargetType="TabItem">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
    </Style>

    <!-- Standard TabControl style -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource PrimaryBackgroundBrush}" />
    </Style>

    <FontFamily x:Key="GridFontFamily">Segoe UI</FontFamily>
    <system:Double x:Key="GridFontSize">13</system:Double>
    <system:Double x:Key="HeaderFontSize">15</system:Double>

    <!-- Toolbar button style: follows skin colors -->
    <Style x:Key="ToolbarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#E8E8E8" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ThemeBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#D0D0D0" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#B8B8B8" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#AAAAAA" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#DDDDDD" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
